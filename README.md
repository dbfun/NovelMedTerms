# Исследовательский фреймворк для обнаружения новой медицинской терминологии в научных публикациях

Этот фреймворк предназначен для исследования и обнаружения новой медицинской терминологии в научных публикациях.

Он имеет модульную архитектуру, поддерживает гибкую настройку рабочих процессов через YAML-файлы, интеграцию с внешними
словарями и автоматический вывод результатов в нескольких форматах.

## Системные требования

Для работы проекта требуется:

* Python 3.12.3 (работа на других версиях не проверялась);
* РСУБД:
    * PostgreSQL 17.6,
    * либо Docker 27.x и выше - для развертывания базы данных в контейнере (рекомендуемый вариант);
* 20 Гб свободного места для хранения словарей UMLS.

## Установка и настройка проекта

Прежде чем начать, склонируйте репозиторий:

```bash
git clone git@github.com:dbfun/NovelMedTerms.git
```

Затем перейдите в каталог проекта:

```bash
cd NovelMedTerms
```

### 1. Создание и активация виртуального окружения

Для этого выполните следующие команды:

```bash
# создание виртуального окружения
python3 -m venv .venv

# активация виртуального окружения
source .venv/bin/activate

# установка всех зависимостей проекта
pip install -r requirements.txt
```

Пример успешной установки, создания и активации виртуального окружения:

[![asciicast](https://asciinema.org/a/k5CNdzCWE9MQhu66j9wSolRz7.svg)](https://asciinema.org/a/k5CNdzCWE9MQhu66j9wSolRz7)

Вся работа с системой происходит в виртуальном окружении, что отображается как `(.venv)` в командной строке.
Если вы вышли из окружения, активируйте его повторно:

```bash
source .venv/bin/activate
```

### 2. Настройка конфигурации

Конфигурация проекта осуществляется через .env-файлы, содержащие подробное описание параметров.

Скопируйте шаблон `.env.dist` из корня проекта в `.env`, затем отредактируйте его. Например, так:

```bash
cp .env.dist .env
vim .env
```

### 3. Запуск базы данных

Для работы требуется БД PostgreSQL, которую можно развернуть в docker-контейнере (рекомендуемый способ):

```bash
docker compose up -d
```

Пример запуска через docker:

[![asciicast](https://asciinema.org/a/MhvfiFEU0ZyjS6p3UDjyThP1n.svg)](https://asciinema.org/a/MhvfiFEU0ZyjS6p3UDjyThP1n)

Также возможно использовать PostgreSQL без контейнера, предварительно создав 2 базы данных:

* `novel-medterms` - рабочая БД,
* `ci-novel-medterms` - БД для тестов.

### 4. Инициализация проекта

Для работы системы необходимо сделать первичную инициализацию: создание структуры БД, скачивание словарей и другие
подготовительные действия.

Все это выполняется автоматически командой:

```bash
python init.py
```

⚠️ Скачивание и преобразование словарей UMLS потребует значительного времени, а также большого свободного места на
диске - см. "Системные требования".

Пример запуска init.py (~25 мин):

[![asciicast](https://asciinema.org/a/N1nudm7K3MOBs0hlM67m2QIN4.svg)](https://asciinema.org/a/N1nudm7K3MOBs0hlM67m2QIN4)

## Работа с системой

В основе работы системы лежит последовательность шагов (рабочий процесс, "workflow"):

* импорт статей;
* извлечение именованных сущностей (NER);
* поиск извлеченных терминов в словарях;
* вывод результатов.

Все эти этапы вынесены в отдельные модули, а их запуск осуществляется скриптом **workflow.py**.

Последовательность выполнения и параметры модулей задаются в управляющем файле **[workflow.yaml](workflow.yaml)**.

Рекомендуется создать копию этого файла для проведения экспериментов, чтобы:

* изменять параметры модулей;
* отключать отдельные модули (например, импорт статей, если они уже были загружены ранее с теми же параметрами);
* заменять сами модули (например, на этапе NER).

### Сценарий использования

1. Скопируйте файл `workflow.yaml` в каталог `workflows`, предназначенный для пользовательских конфигураций.
2. Внесите необходимые изменения:
    * обновите описание эксперимента `experiment.description`;
    * измените конфигурацию этапов в разделе `stages`.
3. Активируйте виртуальное окружение и запустите систему.

Пример:

```bash
# Копирование базового workflow
cp workflow.yaml workflows/variant-1.yaml

# Внесение изменений
code workflows/variant-1.yaml

# Активация виртуального окружения
source .venv/bin/activate

# Запуск системы
LOG_LEVEL=info python workflow.py workflows/variant-1.yaml
```

💡 Совет: использование отдельного файла для каждого эксперимента позволяет сохранить конфигурацию и
результаты, что упрощает анализ и воспроизводимость экспериментов.

## Сценарии тестирования

### Автоматическое тестирование

Находясь в корне проекта, скопируйте и настройте конфигурацию тестовой среды:

```bash
cp tests/.env.dist tests/.env
vim tests/.env
```

Затем запустите тесты:

```bash
pytest
```

Пример успешного запуска тестов:

[![asciicast](https://asciinema.org/a/4vcNMg6hR3WerX8lwjNgAg5N5.svg)](https://asciinema.org/a/4vcNMg6hR3WerX8lwjNgAg5N5)

Подробнее про [авто-тесты](docs/auto-tests.md).

### Ручное тестирование

#### Проверка ключевого рабочего процесса системы

##### 1. Подготовка

Скопируйте базовый workflow во временный файл `manual-test.yaml`:

```bash
cp workflow.yaml workflows/manual-test.yaml
```

Активируйте виртуальное окружение:

```bash
source .venv/bin/activate
```

##### 2. Настройка

В файле `workflows/manual-test.yaml` включите нужные для тестирования этапы в разделе `stages`.

Затем настройте при необходимости параметры модулей.

##### 3. Запуск

Выполните следующую команду, которая выведет всю отладочную информацию:

```bash
LOG_LEVEL=debug python workflow.py workflows/manual-test.yaml
```

💡 Совет: для менее подробного журналирования используйте `LOG_LEVEL=info`.

##### 4. Проверка

Проверьте ожидаемое поведение программы.

**Чек-лист:**

* [ ] статьи корректно импортированы в БД, таблица `articles`;
* [ ] извлечены именованные сущности ("термины") - таблицы `terms`, `article_term_annotations`;
* [ ] таблица `modules` содержит названия модулей, извлекших термины;
* [ ] термины найдены в словарях, результаты сохранены в таблицы `term_dictionary_ref`;
* [ ] таблица `dictionaries` содержит названия словарей, использованных при поиске терминов;
* [ ] результаты сохранены в файл, указанный в конфигурации.

